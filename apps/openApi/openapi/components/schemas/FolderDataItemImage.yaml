allOf:
  - $ref: ./FolderDataItemFileBase.yaml
  - type: object
    properties:
      fileType:
        allOf:
          - $ref: ./FileType.yaml
          - enum:
            - IMAGE
      metadata:
        $ref: ./FolderDataSize.yaml
    required:
      - metadata
      - fileType
