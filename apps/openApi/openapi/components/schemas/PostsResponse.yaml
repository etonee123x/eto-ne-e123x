type: object
properties:
  _meta:
    type: object
    properties:
      cursorPrevious:
        type: integer
        nullable: true
      cursorNext:
        type: integer
        nullable: true
    required:
      - cursorPrevious
      - cursorNext
  rows:
    type: array
    items:
      $ref: ./PostResponse.yaml
required:
  - _meta
  - rows